//! Common type aliases used throughout the engine.

/// Search depth
pub type Depth = u32;

/// Score
pub type Score = i32;

/// Floating-point score
pub type Scoref = f32;

/// Selectivity level for search pruning
pub type Selectivity = u8;
